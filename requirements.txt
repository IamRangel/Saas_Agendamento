# --- Framework ---
Flask==3.1.3
Werkzeug==3.1.7
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.3.1
blinker==1.9.0
typing_extensions==4.15.0

# --- Banco de dados (SQLite local + Postgres no deploy) ---
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.48
Flask-Migrate==4.0.7
alembic==1.14.1
Mako==1.3.8
psycopg[binary]==3.2.13

# --- API / CORS / rate limit ---
Flask-Cors==6.0.2
Flask-Limiter==3.5.0
limits==3.13.0
ordered-set==4.1.0

# --- Autenticação (JWT + hash de senha) ---
python-jose[cryptography]==3.3.0
cryptography==44.0.2
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# --- Configuração e servidor WSGI (obrigatório na nuvem) ---
python-dotenv==1.0.1
gunicorn==23.0.0

# --- Dados de fuso horário (imagens Linux slim / alguns PaaS sem tzdata no SO) ---
tzdata==2025.1
